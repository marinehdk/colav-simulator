# FCB45 完整工程仿真栈：复核与验收计划

2026-09-08。用户确认船舶在建，采用同事推测参数与设计资料；不要求实船标定验收。
基线提交：7e493768。保留既有 V1 简化执行器配置和无关文件。

## 完成条件

1. 修复已复现的 ILOS 换线首帧错误：新 route_id/revision 的投影与全新导引实例一致，旧游标不能污染新航线；原导引/route bridge 回归通过。
2. 新 FCB45 执行器配置：3 主推、2 舵、2 首侧推；主推/侧推力率和舵角速率真实执行；舵力随相对来流和桨后洗流变化，零来流零推力时无舵力；输出实际力、舵角、限幅、侧推锁定证据。控制器 anti-windup 消费实际 achieved load。
3. 首侧推采用同事 ship_config.yaml 的工程参数：1.5 m/s 起降额，3.2 m/s 锁定，低于 2.7 m/s 解锁；高速不能靠首侧推完成避让。
4. 环境复核：流与船体阻尼不能重复计入静水阻力；风、流、一阶波和平均漂移分开取证，4DOF roll 有动态响应；当前推测载荷资产不标记船型实测验证。
5. 4DOF + ILOS + FCB45 PID + 新执行器 + 风浪流从 catalog/runner 实际装配；路由输入确实消费 ILOS，不能用 direct reference 冒充。
6. 三算法 VO/Fan-MPC/Mid-MPC：单目标 OT/HO/CS 与三目标产品场景，分别记录到达、碰撞/搁浅、最小距离、返航 XTE、fallback 与实际执行器证据。沿用既定安全门，不改场景规避新失败、不缩小安全阈值。旧 S10 矩阵是单独基线，包含历史 profile 和 goal override，不能冒充产品场景验收。

## 实施边界

- 最小修复 ILOS 生命周期；不移植同事 ROS 节点、DP 模式、避碰算法或控制策略大包。
- 新物理执行器布局独立于旧恒定横力等效 V1，参数来源与工程假设进入 evidence。
- 舵面积 3.5 m²、升力斜率 2.8/rad、角限 ±0.6109 rad、角速 0.1 rad/s；主推 200 kN/s，侧推 50 kN/s，均来自同事配置。洗流/失速模型须依据调研再冻结，不伪称已标定。
- 分段运行 pytest；保留完整日志和退出码。当前 8010 会话不作为 headless 测试容器。

## 已有证据

- 135 项导引/环境/载荷/旧 FCB45 模块基线通过。
- 新换线复现 2 项失败：旧游标 2 泄漏至新航线首帧；修复后导引及 bridge 批次 55 项通过。
- 原始日志：tmp/fullstack_review_20260908/。
