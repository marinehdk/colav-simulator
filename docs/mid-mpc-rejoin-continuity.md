# Mid-MPC：先恢复航线，再完成终端停车

2026-09-08，基于 `31d37a31`，接续有限终点停车实现。

## 行为

保留避碰义务与终端停车。最后航段、当前义务允许恢复时，提前生成回归航线参考；终点进入 400s 预测窗口不再改变近端参考的生成方式。通过五次曲线匹配当前横向位置与入段方向，回归出口切向与航线一致、曲率为零。转弯空间估计保留减速距离；空间不足时平滑降低回归权重，采用受转速约束的终点接近参考。

终端位置权重从零平滑增加；窗口内无法完成停车时，先以可达参考末点为目标，避免为强行命中终点切掉回归段。最终航点速度为零时，使用上一航点声明的巡航速度保持位置修正能力，避免瞬时名义速度趋零后撤销终端目标。

安全复核通过的旧计划仅保留近端软参考，且只在最后航段允许恢复时跨越 COLREG 权威切换；无效旧计划不复用。停车参考逐步降低连续性代价，速度参考直接跟随新的减速计划。原安全硬约束、求解接受门槛、实际船体执行检查均保留。

## 可验证条件与结果

产品组合：Mid-MPC / God / FCB45 plant + pass-through guidance + FCB45 PID，ideal / calm，seed 0，CPA safe/hard 200/180m，80×5s horizon、10s 求解周期。

“实际回归”要求：已出现避碰、当前义务允许恢复、横向误差 ≤20m、航向误差 ≤8°、距终点仍 >200m。到达保持实测距离 ≤5m、速度 ≤0.05m/s。

| 场景 | 实际回归 | 实际停车 | 终点误差 | 末速 | 最小目标中心距 |
| --- | ---: | ---: | ---: | ---: | ---: |
| OT | 582.5s | 861.0s | 4.9104m | 0.04765m/s | 512.33m |
| HO | 329.5s | 815.5s | 4.2471m | 0.04755m/s | 228.50m |
| CS | 276.5s | 425.5s | 4.1670m | 0.04744m/s | 260.09m |

- 上述三种实际闭环：3 passed，303.13s；每帧 ENC 检查、船间距离、实际到达与 no-fallback 门槛保留。
- 三目标闭环：1 passed，129.11s；保留原会遇顺序、安全、船尾通过与终点条件。额外暴露的零终点速度问题先红后绿。
- 终点、连续性、数值内核、装配、集成、接受门槛、静态安全、生命周期及 parity：231 passed，23.72s。
- 前端现有测试：258 passed；本次未修改前端。
- Ruff check、Ruff format --check、git diff --check：通过。

三种单目标数据采集于最后的零终点速度修复前；该修复对它们的恒定声明速度不改变巡航参考。三目标与 231 项专项均在该修复后验证。

证据目录：`tmp/debug_mid_rejoin/`；单目标 `product_final/`，三目标 `three_zero_fixed/`，专项 `final_focused_zero_fixed.log`，前端 `final_frontend.log`。

## 边界

回归可行性采用有限采样曲率与减速空间估计，属于工程启发式，不构成任意初态、障碍布局或环境下的可行性证明。横向误差已 ≤5m 时直接沿终点方向接近；航向偏差 ≥75° 时暂不构建以航线纵向参数化的回归曲线，先按受转速约束的终点方向调整，后续重算。参考逐步转向及近端软连续性不等于相邻两次非凸优化全局最优解必然连续。

本次验证范围为上述三种单目标产品场景、一个三目标闭环与专项测试；未声明全仓测试、全部会遇或 MASS-L3 系统验收通过。
